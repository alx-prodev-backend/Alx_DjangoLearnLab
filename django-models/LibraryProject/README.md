# LibraryProject

## Overview
LibraryProject is a basic Django project created to get familiar with Django's workflow and structure. This project serves as a starting point for developing Django applications.

## Setup Instructions

1. **Ensure Python is installed** on your system (Python 3.x recommended).
2. **Install Django** using pip:

   ```bash
   pip install django
